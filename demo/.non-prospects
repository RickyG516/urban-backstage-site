# Pages that live in the demo library but are not trade-contractor prospects.
# They never enter HubSpot or the dial queue, and the baseline gate's
# trade-page image rules do not apply to them.
#
# Read by BOTH tools/enrich_audit.py and gate.sh. Add a slug here once and
# both stop flagging it. Do not keep a second copy of this list anywhere.
#
# One slug per line. Blank lines and # comments ignored.
# Justify every addition in a trailing comment — this is a hole in the audit.

ia-cremers-meats-dubuque   # butcher shop, barter pitch, not a trade contractor
lfn-lawncare               # family, kid summer lawn business, standalone link only
il-rj-window-cleaning-t3wnfp    # window cleaning, not a trade-contractor vertical; never logged in HubSpot
il-screenlab-screen-repair-e7dqm2 # screen repair, not a trade-contractor vertical; never logged in HubSpot
il-grape-escape-galena   # Miss Kitty's Grape Escape, Galena IL - wine/martini bar with live music; hospitality, not a trade-contractor vertical. Page is an active hand-built pitch (Ricky sourcing photos), kept in the library, never entered HubSpot.
